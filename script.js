/* ============================================
   Small touches for the page.
   ============================================ */

// Automatically shows the current year in the footer,
// so you never have to update it by hand.
document.getElementById("year").textContent = new Date().getFullYear();
